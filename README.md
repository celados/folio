# Folio

Folio mounts ordinary Ripple components into unrelated Host frameworks through a small DOM and
lifecycle interface.

The current vertical slice contains a framework-neutral core, a React adapter, and a TanStack Start
acceptance app. Package names are private implementation coordinates until the interfaces have been
proven.

```bash
vp install
vp test
vp check
bun run build
```
