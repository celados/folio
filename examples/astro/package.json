{
  "name": "@celados/folio-example-astro",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 3001",
    "preview:e2e": "astro build && astro preview --port 43174 --host 127.0.0.1",
    "build": "astro build",
    "check": "astro check"
  },
  "dependencies": {
    "@celados/folio": "workspace:*",
    "@celados/folio-example-world-development": "workspace:*",
    "@fontsource-variable/inter": "^5.3.0",
    "astro": "^7.1.3",
    "ripple": "^0.3.108"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@celados/folio-vite": "workspace:*",
    "@duckdb/node-api": "^1.5.4-r.1",
    "typescript": "6"
  }
}
